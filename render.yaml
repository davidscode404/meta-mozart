services:
  - type: web
    name: meta-mozart-backend
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: FAL_KEY
        sync: false
      - key: BACKEND_CORS_ORIGINS
        sync: false
    healthCheckPath: /health
